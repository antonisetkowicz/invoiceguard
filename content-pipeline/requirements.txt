# Pipeline /content — wyłącznie darmowe, open-source zależności.
# Instalacja: pip install -r content-pipeline/requirements.txt

# TTS — głosy neuronowe Microsoft Edge, bez klucza API i bez limitu (krok 3)
edge-tts>=7.0.0

# Transkrypcja do napisów słowo-po-słowie, offline na CPU (krok 5)
faster-whisper>=1.1.0

# Statyczny ffmpeg, jeśli nie masz systemowego.
# UWAGA: ten pakiet NIE zawiera ffprobe — do kroku 8 (QA) potrzebny jest
# pełny ffmpeg (brew install ffmpeg / apt install ffmpeg).
imageio-ffmpeg>=0.5.1

# Alternatywa dla faster-whisper (cięższa, wolniejsza) — odkomentuj w razie potrzeby:
# openai-whisper>=20240930

# Fallback TTS offline, gdy nie ma internetu — instalowany osobno:
# piper-tts>=1.2.0
